import { h } from "hyperapp";
import SignUpButton from "./sign-up-button";

export default (props) => (
    <div>
        <div>
            Logo
        </div>

        <SignUpButton />
    </div>
);
